import React from 'react';

import { Container, Content, VerticalBar } from './styles';

const Formula: React.FC = () => {
  return (
    <Container>
      <Content />
      <VerticalBar />
    </Container>
  );
};

export default Formula;
